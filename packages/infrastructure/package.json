{
  "name": "@wishin/infrastructure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@wishin/domain": "workspace:*",
    "appwrite": "^22.0.0"
  }
}
